version: 1
waivers:
  - control_id: CI-DANGER-007
    justification: >
      pull_request_target is required for this workflow pattern; reviewed in ADR-12
      and restricted to label-gated runs only.
    owner: security@example.com
    status: approved
    expires_at: "2026-12-31"
    applies_to:
      - ".github/workflows/special.yml"

  # Common false-positive patterns for keyword-only signal controls.
  # Uncomment and adjust before relying on these in CI.

  # - control_id: SEC-CODEQL-010
  #   justification: >
  #     CodeQL keyword absence is acceptable: an external SAST scanner
  #     (e.g. Semgrep via scan-sast) is the source of truth here.
  #     SAST coverage is asserted via SAST-SEMGREP-064 evidence file.
  #   owner: appsec-team
  #   status: approved
  #   expires_at: "2027-01-31"

  # - control_id: SEC-SECRETS-050
  #   justification: >
  #     Secret scanning runs via the platform-managed scanner (GitHub
  #     secret scanning / Azure DevOps Credential Scanner) and does not
  #     leave a keyword in the workflow YAML. Verified in PLAT evidence.
  #   owner: appsec-team
  #   status: approved
  #   expires_at: "2027-01-31"

  # - control_id: AWS-SCA-040
  #   justification: >
  #     SCA runs out-of-band via the central pipeline orchestrator;
  #     the buildspec does not call pip-audit/snyk directly. Coverage
  #     is asserted by the central scanner audit log.
  #   owner: platform-security
  #   status: approved
  #   expires_at: "2027-01-31"

  # - control_id: CONT-RUNTIME-003
  #   justification: >
  #     The Dockerfile uses `curl ... | bash` only inside a base-image
  #     build stage rebuilt nightly from a pinned digest; the fetched
  #     script is verified by sha256sum against a tracked value before
  #     piping to bash. CONT-IMAGE pins remain enforced.
  #   owner: container-platform
  #   status: approved
  #   expires_at: "2027-01-31"
  #   applies_to:
  #     - "build/Dockerfile.base"

  # - control_id: K8S-PSS-007
  #   justification: >
  #     allowPrivilegeEscalation=false is enforced cluster-wide via a
  #     Kyverno ClusterPolicy in the platform repo (see ADR-37); the
  #     in-repo manifest intentionally omits the field because the
  #     admission controller mutates it. Evidence in
  #     .oss-policy-kit/evidence/k8s-baseline.json reflects the
  #     post-admission posture.
  #   owner: platform-k8s
  #   status: approved
  #   expires_at: "2027-01-31"
